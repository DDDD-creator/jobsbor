# 环境变量配置

## 开发环境 (.env.local)

```bash
# 数据库连接（使用本地或Neon数据库）
DATABASE_URL="postgresql://用户名:密码@主机名/数据库名?sslmode=require"

# 站点配置
NEXT_PUBLIC_SITE_URL="http://localhost:3000"
NEXT_PUBLIC_SITE_NAME="Jobsbor"

# JWT密钥
JWT_SECRET="your-super-secret-jwt-key-change-in-production"

# Stripe 支付配置
STRIPE_SECRET_KEY="sk_test_..."
STRIPE_WEBHOOK_SECRET="whsec_..."
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY="pk_test_..."

# Stripe Price IDs (需要在 Stripe Dashboard 创建产品后填写)
STRIPE_PRICE_PRO_MONTHLY="price_..."
STRIPE_PRICE_PRO_YEARLY="price_..."
STRIPE_PRICE_ENTERPRISE_MONTHLY="price_..."
STRIPE_PRICE_ENTERPRISE_YEARLY="price_..."
```

## 生产环境 (Vercel)

在 Vercel 项目设置 → Environment Variables 中添加：

| 变量名 | 值 | 环境 |
|--------|-----|------|
| `DATABASE_URL` | Neon数据库连接字符串 | Production |
| `NEXT_PUBLIC_SITE_URL` | `https://你的域名.com` | Production |
| `NEXT_PUBLIC_SITE_NAME` | `Jobsbor` | Production |
| `JWT_SECRET` | 随机生成的强密钥 | Production |
| `STRIPE_SECRET_KEY` | `sk_live_...` | Production |
| `STRIPE_WEBHOOK_SECRET` | `whsec_...` | Production |
| `NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY` | `pk_live_...` | Production |
| `STRIPE_PRICE_PRO_MONTHLY` | `price_...` | Production |
| `STRIPE_PRICE_PRO_YEARLY` | `price_...` | Production |
| `STRIPE_PRICE_ENTERPRISE_MONTHLY` | `price_...` | Production |
| `STRIPE_PRICE_ENTERPRISE_YEARLY` | `price_...` | Production |

## 获取 Neon 数据库连接字符串

1. 登录 https://neon.tech
2. 创建项目
3. 在 Dashboard 找到 `Connection String`
4. 复制完整字符串，格式如下：
   ```
   postgresql://用户名:密码@主机名/数据库名?sslmode=require
   ```

## Stripe 配置步骤

### 1. 创建 Stripe 账户
1. 访问 https://stripe.com 注册账户
2. 完成账户验证

### 2. 获取 API Keys
1. 进入 Developers → API keys
2. 复制 Publishable key 和 Secret key
3. 开发环境使用 Test mode keys (以 pk_test_ 和 sk_test_ 开头)
4. 生产环境使用 Live mode keys (以 pk_live_ 和 sk_live_ 开头)

### 3. 创建产品和价格
1. 进入 Products → Add product
2. 创建 Pro 月付产品：
   - 名称: Pro Plan Monthly
   - 价格: $49.00 / month
   - 复制 Price ID 到 STRIPE_PRICE_PRO_MONTHLY
3. 创建 Pro 年付产品：
   - 名称: Pro Plan Yearly
   - 价格: $470.00 / year (20% off)
   - 复制 Price ID 到 STRIPE_PRICE_PRO_YEARLY
4. 同样创建 Enterprise 月付和年付产品

### 4. 配置 Webhook
1. 进入 Developers → Webhooks
2. 添加端点: `https://你的域名.com/api/payments/webhook`
3. 选择以下事件：
   - `payment_intent.succeeded`
   - `payment_intent.payment_failed`
   - `invoice.paid`
   - `invoice.payment_failed`
   - `customer.subscription.created`
   - `customer.subscription.updated`
   - `customer.subscription.deleted`
4. 复制 Webhook signing secret 到 STRIPE_WEBHOOK_SECRET

### 5. 本地开发 Webhook 测试

使用 Stripe CLI:

```bash
# 安装 Stripe CLI
brew install stripe/stripe-cli/stripe

# 登录
stripe login

# 转发 webhook 到本地
stripe listen --forward-to localhost:3000/api/payments/webhook
```

### 6. 测试卡号

- 成功支付: `4242 4242 4242 4242`
- 需要认证: `4000 0025 0000 3155`
- 支付失败: `4000 0000 0000 9995`
- 任意未来日期，任意 3 位 CVC

## 定价方案

| 方案 | 月付 | 年付 | 职位发布 | 推广次数 | 特色 |
|------|------|------|----------|----------|------|
| Free | $0 | $0 | 3个 | 0次 | 基础功能 |
| Pro | $49 | $470 (省$118) | 20个 | 5次 | 数据分析、优先支持 |
| Enterprise | $199 | $1910 (省$478) | 无限 | 20次 | API访问、专属经理 |

## 推广产品价格

| 类型 | 7天 | 14天 | 30天 |
|------|-----|------|------|
| Featured (置顶) | $99 | $169 | $299 |
| Highlight (高亮) | $49 | $89 | $149 |
| Urgent (急聘) | $29 | $49 | $79 |
| Homepage (首页) | $499/天 | $1299/3天 | $2499/7天 |

## 注意事项

- 不要把 `.env.local` 提交到 GitHub（已在 `.gitignore` 中）
- 生产环境密码要复杂，不要使用简单密码
- 定期备份数据库
- 生产环境必须使用 HTTPS
- Webhook 必须验证签名
- 测试模式下不会扣真实款项
